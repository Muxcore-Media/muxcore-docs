{
  "name": "muxcore-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Static site rendered from MuxCore wiki markdown",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "npx --yes serve dist -l 8080",
    "test": "node --test test/*.test.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "marked": "^15.0.12"
  },
  "devDependencies": {
    "axe-core": "^4.10.3",
    "jsdom": "^26.1.0"
  }
}
